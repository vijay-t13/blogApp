<div>
    <h2><%= @blog_post.title %> </h2>
    <p><%= @blog_post.body %> <p>
    <% if user_signed_in? %>
    <%= button_to "edit", edit_blog_post_path, method: :get %>
    <%= button_to "delete", "/blog_posts/#{@blog_post.id}/delete", data: { turbo_method: :post, turbo_confirm: "Are you sure??" } %>
    <% #= button_to "delete", @blog_post, method: :delete %>
    <% end %>
</div>

